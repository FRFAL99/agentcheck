import { describe, it, expect } from "vitest";

describe("invoice", () => {
  it.only("creates", () => { expect(1).toBe(1); });
  it("totals", () => { expect(1).toBe(1); });
  // it.skip("old commented one", () => {});
});
